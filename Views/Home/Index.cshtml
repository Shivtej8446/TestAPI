@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Smart Inventory</title>
    <link rel="stylesheet" href="~/Content/style.css" />
    <style>
        /* 🔒 Logout Button Styling */
        #logoutContainer {
            position: absolute;
            top: 20px;
            right: 30px;
            z-index: 1000;
        }

        #logoutBtn {
            background-color: #ff5252;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 8px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            #logoutBtn:hover {
                background-color: #c62828;
            }
            
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
</head>
<body>
     @*🌌 Video Background 
    <video autoplay muted loop id="bgVideo">
       <source src="~/Content/Videos/login-bg.mp4" type="video/mp4" />
        <source src="@Url.Content("~/Content/Videos/login-bg-6.mp4")" type="video/mp4" />
        Your browser does not support the video tag.
    </video>*@

    @*<button onclick="toggleDarkMode()" id="darkModeToggleBtn"
            style="background-color: teal; color: white; padding: 6px 14px; border: none; border-radius: 6px;">
        🌙 Toggle Dark Mode
    </button>*@

    <!-- 👤 LOGIN SECTION -->
    <div id="loginContainer" style="max-width: 350px; margin: 60px auto; padding: 30px; background-color: #111; border-radius: 10px;">
        <h2 class="section-title">Login</h2>
        <div class="form-container">
            <form id="loginForm">
                <input type="text" id="username" placeholder="Username" required />
                <input type="password" id="password" placeholder="Password" required />
                <button type="submit">Login</button>
            </form>
        </div>
    </div>


    <!-- 📦 INVENTORY SECTION (Initially Hidden) -->
    <div id="inventorySection" style="display: none; position: relative;">

        <!-- 🔝 Navigation Bar (Top Menu) -->
        <div id="topNavBar">
            <div class="nav-left">
                <div style="position: relative;">
                    <button onclick="toggleCategoryDropdown()">Products</button>
                    <div id="categoryDropdown" class="category-list animate__animated" style="display: none;"></div>
                </div>
                <button onclick="showTab('dashboard')">Dashboard</button>
                <button onclick="showTab('history')">History</button>
                <button onclick="showTab('users')">Users</button> <!-- ✅ User Tab -->
            </div>

            <div class="nav-right">
                <button onclick="toggleDarkMode()" id="darkModeToggleBtn">🌙 Dark Mode</button>
                <button onclick="logout()" id="logoutBtn">🔒 Logout</button>
            </div>
        </div>

        <h1 class="main-heading style=" color teal;">Smart Inventory System</h1>
        @*<h1 class="main-heading" style="color: orangered;">Smart Inventory System</h1>*@



        <!-- 📊 Dashboard Section -->
        <div id="dashboardSection" style="display: none; margin-bottom: 30px;">
            <h2 class="section-title" style="text-align: center;">Category-wise Product Distribution</h2>
            <div style="display: flex; justify-content: center; align-items: center; margin-top: 20px;">
                <div style="width: 100%; max-width: 500px; height: 400px;">
                    <canvas id="categoryChart" style="width: 100%; height: 100%;"></canvas>
                </div>
            </div>
        </div>

        <!-- ✅ 👤 USERS SECTION -->
        <div id="usersSection" style="display: none; text-align: center; margin-top: 40px;">
            <h2 class="section-title">Users</h2>
            <p id="loggedInUserInfo" style="font-size: 20px; font-weight: bold;">👤: Admin</p>
        </div>

        <!-- ✅ Product Details with Image on Left -->
        <div class="form-section">
            <h2 class="section-title">Product Details</h2>

            <div style="display: flex; align-items: flex-start; gap: 40px; flex-wrap: wrap;">

                <!-- 🖼️ Left Image -->
                @*<div>
                    <img src="~/Content/Images/Inv-Img2.png" alt="Product"
                         style="width: 500px; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.2);" />
                </div>*@

                <!-- 🧾 Right Form -->
                <!--This has to bee changed.. mainly used for the Img under layout-->
                @*<div class="form-container" style="flex-grow: 1;">

                    <div style="display: flex; gap: 10px; align-items: center; margin-top: 10px;">
                        <input type="text" id="searchBox" placeholder="Search by name or category..."
                               style="width: 300px; padding: 8px; border-radius: 6px; border: 1px solid #ccc;" />
                        <button type="button" onclick="filterProducts()"
                                style="background-color: #1976d2; color: white; padding: 8px 16px; border-radius: 6px; border: none;">
                            🔍 Filter
                        </button>
                    </div>

                    <form id="productForm" style="margin-top: 20px;">
                        <input type="hidden" id="ProductID" />
                        <input type="text" id="ProductName" placeholder="Product Name" required />
                        <input type="text" id="Category" placeholder="Category" required />
                        <input type="number" id="QuantityInStock" placeholder="Quantity" required />
                        <input type="number" id="UnitPrice" placeholder="Unit Price" required />
                        <input type="number" id="CostPrice" placeholder="Cost Price" required />
                        <button type="submit" id="submitBtn">Add Product</button>
                    </form>

                </div>*@

                <!--<div class="form-container" style="display: flex; align-items: flex-start; gap: 40px; margin-top: 20px;">-->

                    @*<div class="form-container" style="display: flex; align-items: flex-start; gap: 60px; margin-top: 30px; max-width: 800px; margin-left: auto; margin-right: auto;">*@
                        <div class="form-container" style="display: flex; justify-content: center; align-items: flex-start; gap: 60px; margin-top: 30px; max-width: 1000px; margin-left: auto; margin-right: auto;">

                            <!-- 🖼️ Image Section (Right side of the form) -->
                            @*<div>
                <img src="~/Content/Images/Inv-Img2.png" alt="Product Image" style="width: 250px; border-radius: 10px; box-shadow: 0 2px 6px rgba(0,0,0,0.3);" />
            </div>*@

                            <div style="flex: 1;">
                                <img src="~/Content/Images/Inv-Img2.png" alt="Product Image"
                                     style="width: 100%; max-width: 360px; border-radius: 10px; box-shadow: 0 2px 6px rgba(0,0,0,0.3);" />
                            </div>

                            <!-- 📋 Form Section (Left side) -->
                            @*<div>
            <div style="display: flex; gap: 10px; align-items: center; margin-bottom: 0px;">
                <input type="text" id="searchBox" placeholder="Search by name or category..."
                       style="width: 300px; padding: 8px; border-radius: 6px; border: 1px solid #ccc;" />
                <button type="button" onclick="filterProducts()"
                        style="background-color: #1976d2; color: white; padding: 8px 16px; border-radius: 6px; border: none;">
                    🔍 Filter
                </button>
            </div>*@

                            <form id="productForm">
                                <input type="hidden" id="ProductID" />
                                <input type="text" id="ProductName" placeholder="Product Name" required />
                                <input type="text" id="Category" placeholder="Category" required />
                                <input type="number" id="QuantityInStock" placeholder="Quantity" required />
                                <input type="number" id="UnitPrice" placeholder="Unit Price" required />
                                <input type="number" id="CostPrice" placeholder="Cost Price" required />
                                <button type="submit" id="submitBtn"> Add Product</button>
                            </form>
                        </div>
                </div>

            </div>
        </div>

    <div id="productTableContainer" style="display: none;">
        <h2 class="section-title">All Products</h2>
        <table>
            <thead>
                <tr>
                    <th>Product</th>
                    <th>Category</th>
                    <th>Stock</th>
                    <th>Unit Price</th>
                    <th>Cost Price</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="productTable"></tbody>
            <div style="margin: 15px 0; text-align: right;">
                <button onclick="exportToCSV()" style="background-color: #388e3c; color: white; padding: 8px 14px; border-radius: 6px; border: none;">
                    📤 Export to CSV
                </button>
            </div>
        </table>
    </div>
 </div>

    <!-- ✅ Toast Message -->
    <div id="toast" class="toast"></div>

    <!-- ✅ Script -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="~/Scripts/script.js"></script>
    
</body>
</html>

